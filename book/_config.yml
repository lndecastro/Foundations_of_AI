# Book settings
# Learn more at https://jupyterbook.org/customize/config.html

title: "Foundations of AI"
author: Leandro Nunes de Castro, Ph.D.
logo: dendritic_logo.png
copyright: "2026"

# Force re-execution of notebooks on each build.
# See https://jupyterbook.org/content/execute.html
execute:
  execute_notebooks: force

# Define the name of the latex output file for PDF builds
latex:
  latex_documents:
    targetname: Foundations_of_AI.tex

# Add a bibtex file so that we can create citations
bibtex_bibfiles:
  - references.bib

# Information about where the book exists on the web
repository:
  url: https://github.com/lndecastro/Foundations_of_AI # Online location of your book ( Change this to new repo name )
  path_to_book: book # Optional path to your book, relative to the repository root
  branch: main # Which branch of the repository should be used when creating links (optional)

html:
  use_repository_button: true
  use_edit_page_button: true
  use_issues_button: false
  static_path:
    - images
    - Data

# Colab badge on every page: students run the code snippets in the cloud,
# with no local Python installation required.
launch_buttons:
  colab_url: "https://colab.research.google.com"
  notebook_interface: "classic"

only_build_toc_files: true

sphinx:
  config:
    html_show_copyright: true
